#include "holberton.h"
/**
  * _strlen - returns the lenght of a string
  * @s: string coming
  * Return: lenght of a string
**/
int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] != '\0'; l++)
	{
	}
	return (l);

}
